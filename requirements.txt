flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0
# Pinned below 2.0: haldir_mcp_server.py targets the mcp 1.x Server API
# (server.list_tools / server.call_tool / create_initialization_options).
# mcp 2.0 replaced that surface with add_request_handler / get_request_handler,
# so an unpinned install resolves to 2.x and the MCP server fails to start —
# which is what a fresh `pip install` got. Migrating to the 2.x API is a
# follow-up; this pin is what keeps new installs working until then.
mcp>=1.0.0,<2.0.0
httpx>=0.27.0
pydantic>=2.0.0
cryptography>=42.0.0
psycopg2-binary>=2.9.0
stripe>=8.0.0
